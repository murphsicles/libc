[package]
name = "@ffi/libc"
version = "1.0.0-alpha.3"
description = "Raw FFI bindings to platform libraries for Zeta — errno, stat, open, socket, dirent, pthread"
license = "MIT"
repository = "https://github.com/murphsicles/libc"
readme = "README.md"
